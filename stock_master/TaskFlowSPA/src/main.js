import "./styles/global.css";
import { renderRoute, initRouter } from "./router/router";

// renderRoute() pinta la vista según la URL actual al cargar.
// initRouter() registra los listeners de navegación y vuelve a renderizar.
renderRoute()
initRouter()
